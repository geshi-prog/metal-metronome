/**
 * ファイル名：src/hooks/useTapTempo.ts
 * 目的：TAPテンポ（ユーザーがクリック／タップした間隔からBPMを計算）を行うカスタムフック
 *
 * 機能概要：
 * - ユーザーのクリック間隔（時刻）を記録
 * - 一定回数以上の平均を取り、BPMを算出
 * - タイムアウトで入力をリセット
 *
 * 今後の拡張予定：
 * - タップ時のフィードバック表示（拍の回数や現在BPM）
 * - 直前何回分で平均するかのカスタム対応
 */
